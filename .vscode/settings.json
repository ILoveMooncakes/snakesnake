{
  // Use the project's .venv Python interpreter if present
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  // Automatically activate the environment in the integrated terminal
  "python.terminal.activateEnvironment": true,
  // Format-on-save optional preference
  "editor.formatOnSave": false
}
